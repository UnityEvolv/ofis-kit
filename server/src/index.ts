/**
 * The single-office host.
 *
 * The engine is wired up here: the realtime core with the in-memory presence
 * store, the typed-email identity adapter, the template read off disk and the
 * built-in peer-to-peer provider. That wiring arrives with the realtime service
 * skeleton story; this file exists so the directory is real and the workspace
 * builds.
 */
export {}
